// This file contains the JavaScript code for the application. It handles the interactive functionality and manipulates the DOM.

document.addEventListener('DOMContentLoaded', () => {
    const greetingElement = document.createElement('h1');
    greetingElement.textContent = 'Welcome to My Frontend App!';
    document.body.appendChild(greetingElement);
});